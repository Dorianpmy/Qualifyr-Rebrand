/**
 * Composant retiré.
 *
 * Son propos est repris par `ServiceTabs` et `AgentFlow`. Le fichier est
 * conservé vide le temps d'un commit pour que la suppression apparaisse
 * clairement dans l'historique ; il peut être effacé ensuite.
 */

export {};
