// The diagnostic form has been retired along with the /diagnostic page.
// This file could not be deleted because the sandbox blocked file deletion
// (unlink returned "Operation not permitted"), so it is neutralized here
// instead. Nothing in the codebase imports this module anymore.
export {};
